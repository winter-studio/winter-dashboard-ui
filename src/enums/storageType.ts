enum StorageType {
  REFRESH_TOKEN = 'REFRESH_TOKEN', // REFRESH_TOKEN
  ACCESS_TOKEN = 'ACCESS_TOKEN', // 用户token
  CURRENT_USER = 'CURRENT_USER', // 当前用户信息
  TABS_ROUTES = 'TABS_ROUTES' // 切换标签页
}

export default StorageType
